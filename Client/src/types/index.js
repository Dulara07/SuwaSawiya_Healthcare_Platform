// Types were used in TypeScript only. This file is left as a placeholder
// to avoid import errors during conversion. Remove unused imports if desired.

// Optionally add JSDoc typedefs here for editor hints.

export const __TYPES_PLACEHOLDER = true;
